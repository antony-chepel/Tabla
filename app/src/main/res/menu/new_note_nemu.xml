<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="Save"
        android:id="@+id/save_note"
        app:showAsAction="ifRoom"
        android:orderInCategory="300"
        android:icon="@drawable/ic_save_note"
        />

    <item android:title="Bold"
        android:id="@+id/bold_style"
        app:showAsAction="ifRoom"
        android:orderInCategory="200"
        android:icon="@drawable/ic_bold"
        />

    <item android:title="Color_picker"
        android:id="@+id/color_picker"
        app:showAsAction="ifRoom"
        android:orderInCategory="100"
        android:icon="@drawable/ic_color_picker"
        />

</menu>